# Dependencies for the email-ingestion poller image only. It pulls the
# Cloudflare queue, fetches R2 objects, parses receipt emails, and writes CSVs /
# Markdown — no browser automation, so Playwright/Chromium are intentionally
# omitted (keeps the image small).
httpx[http2]>=0.27
boto3>=1.34

# Barcode generation for email receipts (pure Python, no Pillow).
python-barcode>=0.15
